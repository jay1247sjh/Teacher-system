package com.txq.interfaces;

import com.txq.common.BaseApplication;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * 表格模块启动类
 */
public class TableApplication extends BaseApplication {

    public static void main(String[] args) {
        new TableApplication().run(args);
    }
}
