# 开发环境配置示例
# 
# 使用方法：
# 1. 复制此文件并重命名为 .env.development
# 2. 根据实际情况修改配置
# 
# Windows PowerShell:
#   Copy-Item env.development.example .env.development
# 
# Linux/Mac:
#   cp env.development.example .env.development

# 附件服务器地址（用于存放用户头像等附件）
# 如果头像字段不是完整URL，会自动拼接此地址
# 注意：需要加上 /api/v1 前缀，因为 Gateway 的所有路由都以此开头
VITE_ATTACHMENT_BASE_URL=http://localhost:10001/api/v1/attachments/

